-- SOUNDNESS: record update with wrong types.
--
-- Expected rejection: type error.

inductive Nat where
  | zero : Nat
  | suc : Nat -> Nat

-- BAD: trying to use Nat where Empty is expected.
def bad_sigma : Nat := Nat
