-- SOUNDNESS: trying to use Nat where Type is expected.
--
-- Expected rejection: type mismatch.

inductive Nat where
  | zero : Nat
  | suc : Nat -> Nat

-- BAD: Nat is a type, not a universe. This should fail.
def bad_cumul : Type := zero
