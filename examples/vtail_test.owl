-- Simple indexed type test
inductive Nat where
  | zero : Nat
  | suc : Nat -> Nat

inductive Vec (A : Type) (n : Nat) where
  | nil : Vec A zero
  | cons : A -> Vec A n -> Vec A (suc n)

def vtail : forall (A : Type), forall (n : Nat), Vec A (suc n) -> Vec A n :=
  fun A n v => match v return Vec A n with
  | nil => nil
  | cons x xs => xs
